
aStr = '"quote" without escape'
print(aStr)
aStr = "'quote' without escape"
print(aStr)

-- Both are equivalent; 
-- the only difference is that inside each kind of quote we can use the other quote without escapes.
-- Courtesy Programming in Lua

